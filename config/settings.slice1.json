{
  "_comment": "SLICE 1 - ~/.claude/settings.json IN de WSL2-distro. Alleen om snel te itereren; dit is GEEN security boundary, want de developer bezit dit bestand. Twee lagen, want de sandbox dekt alleen Bash en zijn subprocessen: permissions.deny beschermt Claude's eigen Read-tool, sandbox.filesystem beschermt alles wat Bash start. Paden zijn voorlopig - definitief zodra OQ-1 en OQ-2 beantwoord zijn.",
  "_paden": "DIT IS DE ENIGE PLEK WAAR PADEN STAAN. Wat Willem straks aanpast zijn de vier lijsten hieronder: permissions.deny (tools), sandbox.filesystem.denyRead / allowRead / allowWrite (Bash). Houd de eerste twee in sync - ze beschermen dezelfde paden op twee verschillende lagen.",
  "_beschermd": {
    "~/probe-a": "Read(~/probe-a/**)",
    "~/.ssh": "Read(~/.ssh/**)",
    "~/.aws": "Read(~/.aws/**)",
    "/mnt/": "Read(//mnt/**)",
    "~/repos/**/config-a.txt": "Read(~/repos/**/config-a.txt)",
    "~/repos/**/submap": "Read(~/repos/**/submap/**)"
  },
  "_beschermd_uitleg": "DE ENIGE PLEK OM EEN BESCHERMD PAD TOE TE VOEGEN. Sleutel = het pad zoals de sandbox-laag het kent, waarde = de Read(...)-regel die dezelfde bescherming op de tool-laag geeft. check-configs.sh eist dat elke waarde in permissions.deny staat en dat de sleutel door denyRead gedekt is - letterlijk of via een bredere wortel. Voeg je hier een pad toe zonder het in beide lijsten te zetten, dan faalt de check.",
  "permissions": {
    "deny": [
      "Read(~/probe-a/**)",
      "Read(~/.ssh/**)",
      "Read(~/.aws/**)",
      "Read(//mnt/**)",
      "Read(~/repos/**/config-a.txt)",
      "Read(~/repos/**/submap/**)"
    ]
  },
  "sandbox": {
    "enabled": true,
    "autoAllowBashIfSandboxed": true,
    "failIfUnavailable": true,
    "allowUnsandboxedCommands": false,
    "filesystem": {
      "denyRead": [
        "~/",
        "/mnt/",
        "~/repos/**/config-a.txt",
        "~/repos/**/submap"
      ],
      "allowRead": [
        "~/repos",
        "/usr",
        "/bin",
        "/sbin",
        "/lib",
        "/lib64",
        "/etc",
        "/opt",
        "/tmp",
        "/proc",
        "/dev",
        "~/.nvm",
        "~/.npm",
        "~/.gitconfig"
      ],
      "allowWrite": [
        "~/repos",
        "/tmp"
      ]
    },
    "network": {
      "_unixsockets": "Standaard blokkeert de sandbox Unix-sockets, maar dat lukt alleen met de optionele seccomp-filter (npm install -g @anthropic-ai/sandbox-runtime). Zonder die filter kan een sandboxed commando via een Unix-socket een Windows-binary starten die op de Windows-kant leest, en daar helpt denyRead op /mnt/ niet. Zet allowAllUnixSockets NIET op true tenzij je die route bewust toestaat. AC-24 meet het.",
      "allowedDomains": [
        "registry.npmjs.org",
        "api.nuget.org",
        "*.nuget.org",
        "github.com",
        "*.githubusercontent.com",
        "api.anthropic.com"
      ]
    }
  }
}
